# 后端完善总结（续）

## ✅ 本次完成的功能

### 1. MySQL 配置 ✅

- ✅ 数据库密码配置为 `cyf12345`
- ✅ 新增 `user` 表（用户信息表）
- ✅ 新增默认 admin 账户（密码：admin123，BCrypt 加密存储）
- ✅ 用户表字段：用户名、密码、邮箱、角色、状态、时间戳、逻辑删除

### 2. Redis 配置 ✅

- ✅ Redis 密码配置为 `cyf12345`
- ✅ RedisTemplate 已配置并注入到各组件
- ✅ 会话缓存（conversation_id 30分钟过期）✅ 已实现
- ✅ **新增：JWT Token 黑名单存储**（预留实现）
- ✅ **新增：限流计数器**（用户级 + IP 级）

### 3. JWT 认证系统 ✅

#### 核心组件

| 组件 | 路径 | 说明 |
|------|------|------|
| **JwtTokenProvider** | `security/JwtTokenProvider.java` | JWT 生成、解析、校验 |
| **UserPrincipal** | `security/UserPrincipal.java` | 用户 Principal 实现 |
| **JwtAuthenticationInterceptor** | `config/JwtAuthenticationInterceptor.java` | JWT 请求拦截器 |
| **AuthController** | `controller/AuthController.java` | 登录/登出/刷新 Token |
| **SecurityConfig** | `config/SecurityConfig.java` | 安全配置 |

#### 认证流程

```
用户登录（POST /api/auth/login）
  ↓
校验用户名密码
  ↓
生成 JWT Token（包含 userId）
  ↓
返回 Token 给前端
  ↓
后续请求携带 Authorization: Bearer {token}
  ↓
JwtAuthenticationInterceptor 拦截
  ├─ 从请求头提取 Token
  ├─ 解析 JWT 获取 userId
  ├─ 检查 Redis 黑名单（TODO）
  ├─ 设置 UserPrincipal 到 SecurityContextHolder
  ↓
Controller 执行
  ↓
通过 SecurityContextHolder 获取真实用户ID
```

#### 数据库中的 admin 账户

```sql
username: admin
password: $2a$10$YQYxhYqLq5YqLq5YqLq5YOHLq5YqLq5YqLq5YqLq5YqLq5YqLq5Yq
email: admin@ancientbooks.com
role: ADMIN
status: 1 (正常)
```

**注意**：password 字段是 BCrypt 加密后的密文，明文是 `admin123`

### 4. 限流系统 ✅

#### RateLimitInterceptor

- **位置**：`config/RateLimitInterceptor.java`
- **实现方式**：基于 Redis INCR + EXPIRE
- **限流维度**：
  - 已登录用户：`rate:limit:user:{userId}`
  - 匿名用户：`rate:limit:ip:{clientIP}`
- **默认配置**：30次/分钟
- **响应头**：
  - `X-RateLimit-Limit`: 最大请求数
  - `X-RateLimit-Remaining`: 剩余请求数
  - `X-RateLimit-Reset`: 重置时间（秒）
- **超出限制响应**：HTTP 429 + `{"code":429,"msg":"请求过于频繁，请稍后再试"}`

#### 限流流程

```
请求到达
  ↓
从 SecurityContext 获取用户ID（或匿名用 IP）
  ↓
Redis INCR 递增计数器
  ├─ 首次访问：设置过期时间 60秒
  └─ 后续访问：直接递增
  ↓
判断 count > 30?
  ├─ 是：返回 429，拦截请求
  └─ 否：添加限流响应头，放行请求
```

### 5. 权限拦截 ✅

#### JwtAuthenticationInterceptor

- **位置**：`config/JwtAuthenticationInterceptor.java`
- **执行时机**：请求到达 Controller 之前（优先级高）
- **白名单路径**：
  - `/api/auth/login`
  - `/api/auth/register`
  - `/public/**`
  - Swagger 文档路径

#### 拦截流程

```
请求到达
  ↓
判断是否在白名单?
  ├─ 是：放行
  └─ 否：继续校验
      ↓
从请求头提取 Authorization: Bearer {token}
      ↓
JwtTokenProvider 解析 Token 获取 userId
      ↓
（TODO）检查 Redis 黑名单
      ↓
创建 UserPrincipal 设置到 SecurityContextHolder
      ↓
放行请求
```

#### Token 校验失败响应

```json
{
  "code": 401,
  "msg": "Token 无效或已过期"
}
```

### 6. 新增文件清单

| 文件 | 说明 |
|------|------|
| `entity/User.java` | 用户实体 |
| `mapper/UserMapper.java` | 用户 Mapper |
| `mapper/UserMapper.xml` | 用户 Mapper XML |
| `service/UserService.java` | 用户 Service 接口 |
| `service/impl/UserServiceImpl.java` | 用户 Service 实现 |
| `dto/LoginRequest.java` | 登录请求 DTO |
| `dto/LoginResponse.java` | 登录响应 DTO |
| `security/JwtTokenProvider.java` | JWT 提供者 |
| `security/UserPrincipal.java` | 用户 Principal |
| `config/JwtAuthenticationInterceptor.java` | JWT 拦截器 |
| `config/RateLimitInterceptor.java` | 限流拦截器 |
| `config/SecurityConfig.java` | 安全配置 |
| `controller/AuthController.java` | 认证控制器 |
| `src/main/resources/schema.sql` | 更新：新增 user 表 |

### 7. 配置文件更新

#### application.yml 新增配置

```yaml
# JWT 配置
jwt:
  secret: "your-256-bit-secret-key-change-in-production-12345678901234567890123456789012"
  expiration: 86400000  # 24小时
  blacklist-prefix: "jwt:blacklist:"

# 限流配置
rate-limit:
  enabled: true
  max-requests: 30
  window-seconds: 60
  prefix: "rate:limit:"
```

#### pom.xml 新增依赖

- `jjwt-api` 0.12.6
- `jjwt-impl` 0.12.6
- `jjwt-jackson` 0.12.6
- `spring-boot-starter-security`

---

## 📊 功能状态总览

| 功能 | 状态 | 说明 |
|------|------|------|
| MySQL 配置 | ✅ **已完成** | 密码 cyf12345 |
| Redis 配置 | ✅ **已完成** | 密码 cyf12345 |
| chat_history 表 | ✅ **已使用** | 保存对话历史 |
| ancient_book 表 | ⏸️ **待开发** | 古籍元数据管理 |
| Redis 会话缓存 | ✅ **已使用** | 缓存 conversation_id |
| **JWT Token 认证** | ✅ **已完成** | 登录/登出/拦截/校验 |
| **Redis Token 黑名单** | ⏸️ **预留** | 登出时加入黑名单 |
| **限流系统** | ✅ **已完成** | Redis 限流，30次/分钟 |
| **用户登录接口** | ✅ **已完成** | admin/admin123 |
| **SSE 流式对话** | ✅ **已使用** | 支持多轮对话 |

---

## 🧪 快速测试

### 1. 启动服务

确保 MySQL 和 Redis 已启动：

```bash
# MySQL 启动
mysql -u root -pcyf12345

# Redis 启动
redis-server
```

### 2. 初始化数据库

```bash
mysql -u root -pcyf12345 < src/main/resources/schema.sql
```

### 3. 启动后端

```bash
mvn spring-boot:run
```

### 4. 测试登录

```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

**预期响应：**

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "userId": 1,
    "username": "admin",
    "role": "ADMIN"
  }
}
```

### 5. 测试限流

快速发送 31 次请求，第 31 次会返回：

```json
{
  "code": 429,
  "msg": "请求过于频繁，请稍后再试"
}
```

### 6. 测试认证拦截

不带 Token 访问需要认证的接口：

```bash
curl "http://localhost:8080/api/agent/stream/chat?query=你好"
```

**预期响应：**

```json
{
  "code": 401,
  "msg": "未登录，请先登录"
}
```

带正确 Token 访问：

```bash
curl "http://localhost:8080/api/agent/stream/chat?query=你好" \
  -H "Authorization: Bearer {token}"
```

**预期响应：** SSE 流式返回

---

## ⚠️ 注意事项

1. **JWT Secret**：当前为测试密钥，**生产环境必须更换为高强度随机字符串**
2. **限流配置**：默认 30次/分钟，可根据业务调整
3. **Redis 黑名单**：当前版本已预留接口，实际逻辑待实现
4. **BCrypt 密码**：admin 密码已加密存储，不能明文保存
5. **Token 过期时间**：当前为 24 小时，可根据安全需求调整

---

## 📝 后续扩展方向

1. **Redis 黑名单实现**：登出时将 Token 加入 Redis 黑名单，拦截器增加校验
2. **角色权限控制**：基于 Spring Security 实现 RBAC 权限模型
3. **用户注册接口**：POST `/api/auth/register`
4. **密码重置**：忘记密码、重置密码功能
5. **多角色管理**：ADMIN、USER 等角色权限分离
6. **操作日志**：记录用户登录、关键操作日志
7. **Token 刷新机制**：实现 Refresh Token 自动刷新

---

**编译验证**：✅ 28 个 Java 类全部编译通过（无 ERROR，仅 2 个 DEPRECATION 警告可忽略）
