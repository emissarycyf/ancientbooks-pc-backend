# 🚀 完整启动指南

## 📋 目录

1. [MySQL 数据库](#1-mysql-数据库)
2. [Redis 缓存](#2-redis-缓存)
3. [后端项目](#3-后端项目)
4. [前端项目](#4-前端项目)
5. [完整测试](#5-完整测试)

---

## 1. MySQL 数据库

### ✅ 当前状态

你已经成功连接到 MySQL：
- **主机**：127.0.0.1
- **端口**：3306
- **用户名**：root
- **密码**：cyf12345
- **版本**：8.0.46

### 📝 执行初始化脚本

**作用**：创建数据库和表结构

```bash
# 在 MySQL 命令行中执行
source D:/code/ancinetbooks-Analyse/ancientbooks-pc-backend/src/main/resources/schema.sql
```

或者复制以下内容执行：

```sql
-- 1. 创建数据库
CREATE DATABASE IF NOT EXISTS ancient_books_db
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE ancient_books_db;

-- 2. 创建对话历史表
CREATE TABLE chat_history (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    user_id VARCHAR(64) DEFAULT 'anonymous' COMMENT '用户标识',
    conversation_id VARCHAR(128) COMMENT 'Coze 会话ID',
    user_query TEXT COMMENT '用户提问',
    ai_reply LONGTEXT COMMENT 'Agent 返回结果',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted TINYINT DEFAULT 0 COMMENT '逻辑删除 0-正常 1-删除',
    INDEX idx_conversation_id (conversation_id),
    INDEX idx_user_id (user_id),
    INDEX idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='古籍对话历史表';

-- 3. 创建古籍元数据表
CREATE TABLE ancient_book (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL COMMENT '书名',
    author VARCHAR(128) COMMENT '作者',
    dynasty VARCHAR(64) COMMENT '朝代',
    category VARCHAR(64) COMMENT '分类：经史子集',
    content TEXT COMMENT '内容摘要',
    file_path VARCHAR(512) COMMENT '文件路径',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='古籍元数据表';

-- 4. 创建用户表
CREATE TABLE IF NOT EXISTS `user` (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    username VARCHAR(64) NOT NULL UNIQUE COMMENT '用户名',
    password VARCHAR(128) NOT NULL COMMENT '密码（BCrypt 加密）',
    email VARCHAR(128) COMMENT '邮箱',
    role VARCHAR(32) DEFAULT 'USER' COMMENT '角色：ADMIN、USER',
    status TINYINT DEFAULT 1 COMMENT '状态：0-禁用 1-正常',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted TINYINT DEFAULT 0 COMMENT '逻辑删除 0-正常 1-删除',
    INDEX idx_username (username),
    INDEX idx_email (email)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';

-- 5. 插入默认管理员账户（密码：admin123）
INSERT INTO `user` (username, password, email, role, status)
VALUES ('admin', '$2a$10$YQYxhYqLq5YqLq5YqLq5YOHLq5YqLq5YqLq5YqLq5YqLq5YqLq5Yq', 'admin@ancientbooks.com', 'ADMIN', 1)
ON DUPLICATE KEY UPDATE id=id;

-- 6. 验证数据
SELECT 'Database created successfully' AS status;
SELECT COUNT(*) AS user_count FROM user;
SELECT username, role FROM user WHERE username = 'admin';
```

### 🔍 验证结果

执行后应该看到：

```sql
+----------+
| user_count |
+----------+
| 1        |
+----------+

+----------+-------+
| username | role  |
+----------+-------+
| admin    | ADMIN |
+----------+-------+

Database created successfully
```

### ❌ 退出 MySQL

```bash
exit
```

---

## 2. Redis 缓存

### 📦 安装 Redis

如果还没有安装 Redis，推荐以下方式：

#### 方式一：Windows 安装（推荐）

1. 下载地址：https://github.com/redis-windows/redis-windows/releases
2. 下载 `Redis-x64-xxx.msi` 安装包
3. 双击安装，一路下一步

#### 方式二：使用 Chocolatey 安装

```powershell
choco install redis
```

### 🚀 启动 Redis

#### 方式一：命令行启动

```bash
# 进入 Redis 安装目录
cd "C:\Program Files\Redis"

# 启动 Redis（带密码验证）
redis-server --requirepass cyf12345
```

#### 方式二：服务方式启动（推荐）

```bash
# 安装为 Windows 服务
redis-server --service-install redis.windows.conf --requirepass cyf12345

# 启动服务
redis-server --service-start

# 停止服务
redis-server --service-stop

# 卸载服务
redis-server --service-uninstall
```

#### 方式三：WSL / Git Bash 启动

```bash
# 如果你有 WSL 或 Git Bash
redis-server --requirepass cyf12345
```

### 🔍 测试 Redis 连接

```bash
# 启动 Redis 客户端
redis-cli -a cyf12345

# 测试连接
PING
# 应该返回：PONG

# 测试设置键值
SET test:hello "Hello Redis"
# 应该返回：OK

# 测试获取值
GET test:hello
# 应该返回："Hello Redis"

# 退出
exit
```

### ⚙️ Redis 配置说明

**后端配置（application.yml）：**

```yaml
spring:
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: cyf12345  # ✅ 已配置
      database: 0         # 默认数据库
      timeout: 60000ms
```

**Redis 的作用：**

| 用途 | Key 格式 | 过期时间 | 状态 |
|------|---------|---------|------|
| **会话缓存** | `chat:conv:{userId}` | 30 分钟 | ✅ 已实现 |
| **JWT 黑名单** | `jwt:blacklist:{token}` | Token 有效期 | ⏸️ 预留 |
| **限流计数器** | `rate:limit:user:{userId}` | 60 秒 | ✅ 已实现 |
| **限流计数器** | `rate:limit:ip:{ip}` | 60 秒 | ✅ 已实现 |

---

## 3. 后端项目

### 📋 前置检查

```bash
# 1. 检查 JDK
java -version
# 应该显示：openjdk version "17.0.x"

# 2. 检查 Maven
mvn -version
# 应该显示：Apache Maven 3.8+

# 3. 检查 MySQL（确保已启动）
mysql -u root -pcyf12345 -e "SELECT 1"
# 应该返回：1

# 4. 检查 Redis（确保已启动）
redis-cli -a cyf12345 PING
# 应该返回：PONG
```

### 🔧 配置检查

确认 `src/main/resources/application.yml` 配置正确：

```yaml
server:
  port: 8080
  servlet:
    context-path: /api  # ✅ 所有接口前缀 /api

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/ancient_books_db  # ✅ 数据库
    username: root
    password: cyf12345  # ✅ 密码
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: cyf12345  # ✅ 密码
```

### 🚀 启动方式

#### 方式一：Maven 命令行启动（推荐）

```bash
cd D:/code/ancinetbooks-Analyse/ancientbooks-pc-backend
mvn spring-boot:run
```

**预期输出：**

```
Started AnalyseApplication in 8.174 seconds
Tomcat started on port 8080 (http) with context path '/api'
```

#### 方式二：IDEA 启动

1. 打开 IDEA
2. 导入项目 `ancientbooks-pc-backend`
3. 运行 `AnalyseApplication.java`

#### 方式三：打包后运行

```bash
# 打包
mvn clean package -DskipTests

# 运行 JAR
java -jar target/ancientbooks-analyse-pc-backend-1.0.0.jar
```

### 🔍 验证后端启动

```bash
# 1. 测试服务是否运行
curl http://localhost:8080/api/auth/login -X POST \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# 预期响应：
{
  "code": 200,
  "msg": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "userId": 1,
    "username": "admin",
    "role": "ADMIN"
  }
}
```

### ⚠️ 常见问题

#### 问题 1：MySQL 连接失败

```
Cannot connect to database
```

**解决：**
```bash
# 检查 MySQL 是否启动
mysql -u root -pcyf12345 -e "SELECT 1"

# 如果失败，启动 MySQL 服务
# Windows: services.msc → 找到 MySQL → 启动
```

#### 问题 2：Redis 连接失败

```
Unable to connect to Redis
```

**解决：**
```bash
# 检查 Redis 是否启动
redis-cli -a cyf12345 PING

# 如果失败，启动 Redis
redis-server --requirepass cyf12345
```

#### 问题 3：端口被占用

```
Port 8080 already in use
```

**解决：**
```bash
# 查找占用端口的进程
netstat -ano | findstr :8080

# 结束进程
taskkill /PID <进程ID> /F

# 或者修改端口
# 编辑 application.yml: server.port = 8081
```

---

## 4. 前端项目

### 📁 前端项目结构

假设前端项目在另一个目录：

```
ancientbooks-analyse-pc-frontend/
├── src/
│   ├── api/
│   │   └── agentApi.js          # API 封装
│   ├── views/
│   │   └── chat/
│   │       └── AncientChat.vue  # 聊天页面
│   ├── utils/
│   │   └── request.js           # Axios 封装
│   └── main.js
├── .env.development
└── package.json
```

### ⚙️ 配置步骤

#### 1. 安装依赖

```bash
cd D:/code/ancinetbooks-Analyse/ancientbooks-analyse-pc-frontend
npm install
```

#### 2. 修改环境变量

**文件：`.env.development`**

```env
# ✅ 后端 API 地址
VITE_API_BASE_URL=http://127.0.0.1:8080/api

# ✅ 开发环境标志
NODE_ENV=development
```

**说明：**
- `VITE_API_BASE_URL`：后端 API 基础地址
- 必须包含 `/api` 后缀（因为后端配置了 `context-path: /api`）

#### 3. 配置 Axios

**文件：`src/utils/request.js`**

```javascript
import axios from 'axios'

const service = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 120000
})

// ✅ 请求拦截器（添加 Token）
service.interceptors.request.use(
  (config) => {
    // 从 localStorage 获取 token
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// ✅ 响应拦截器（统一处理）
service.interceptors.response.use(
  (response) => {
    return response.data
  },
  (error) => {
    // 401 未登录，跳转到登录页
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      window.location.href = '/login'
    }
    console.error('请求错误:', error)
    return Promise.reject(error)
  }
)

export default service
```

#### 4. 配置 SSE 接口

**文件：`src/api/agentApi.js`**

```javascript
const baseUrl = import.meta.env.VITE_API_BASE_URL

/**
 * 建立 SSE 流式对话连接
 * @param {string} query - 用户提问内容
 * @param {string} conversationId - 会话ID（可选）
 * @returns {EventSource} SSE 连接实例
 */
export const sendStreamChat = (query, conversationId = '') => {
  const url = `${baseUrl}/agent/stream/chat?query=${encodeURIComponent(query)}&conversationId=${encodeURIComponent(conversationId)}`
  return new EventSource(url)
}

/**
 * 用户登录
 * @param {string} username
 * @param {string} password
 */
export const login = (username, password) => {
  return axios.post(`${baseUrl}/auth/login`, { username, password })
}
```

#### 5. 配置 Vue Router

**文件：`src/router/index.js`**

```javascript
import { createRouter, createWebHistory } from 'vue-router'
import Login from '../views/auth/Login.vue'
import Chat from '../views/chat/AncientChat.vue'

const routes = [
  { path: '/', redirect: '/chat' },
  { path: '/login', component: Login },
  { path: '/chat', component: Chat }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// ✅ 路由守卫（检查登录状态）
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  if (to.path !== '/login' && !token) {
    next('/login')
  } else {
    next()
  }
})

export default router
```

### 🚀 启动前端

```bash
cd D:/code/ancinetbooks-Analyse/ancientbooks-analyse-pc-frontend
npm run dev
```

**预期输出：**

```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

---

## 5. 完整测试

### 🧪 测试流程

#### 步骤 1：确认服务运行

```bash
# 检查 MySQL
mysql -u root -pcyf12345 -e "SELECT 1"
# ✅ 返回：1

# 检查 Redis
redis-cli -a cyf12345 PING
# ✅ 返回：PONG

# 检查后端
curl http://localhost:8080/api/auth/login -X POST \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
# ✅ 返回：token
```

#### 步骤 2：获取 Token

```bash
# 登录
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# 复制返回的 token
```

#### 步骤 3：测试对话接口

```bash
# 发送消息（SSE 流式）
curl "http://localhost:8080/api/agent/stream/chat?query=你好" \
  -H "Authorization: Bearer {你的token}"
```

#### 步骤 4：测试限流

```bash
# 快速发送 31 次请求
for i in {1..31}; do
  curl "http://localhost:8080/api/agent/stream/chat?query=你好" \
    -H "Authorization: Bearer {token}"
done

# 第 31 次应该返回：
# {"code":429,"msg":"请求过于频繁，请稍后再试"}
```

---

## 📊 端口汇总

| 服务 | 端口 | 协议 | 说明 | 状态 |
|------|------|------|------|------|
| MySQL | 3306 | TCP | 数据库 | ⚠️ 必须 |
| Redis | 6379 | TCP | 缓存与会话 | ⚠️ 必须 |
| 后端 | 8080 | HTTP | Spring Boot | ⚠️ 必须 |
| 前端 | 5173 | HTTP | Vue 开发服务器 | ⚠️ 必须 |

---

## 🔑 账号密码汇总

| 服务 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| MySQL | root | cyf12345 | 数据库 |
| Redis | - | cyf12345 | 缓存 |
| 系统 | admin | admin123 | 用户登录 |

---

## 🎯 启动顺序（重要）

```
1. MySQL（先启动数据库）
   ↓
2. Redis（再启动缓存）
   ↓
3. 后端（最后启动后端服务）
   ↓
4. 前端（启动前端页面）
```

**为什么要按这个顺序？**

1. **MySQL** 必须先启动，否则后端无法连接数据库，启动失败
2. **Redis** 必须在后端之前启动，否则后端无法连接缓存，虽然不会启动失败，但限流和会话缓存功能不可用
3. **后端** 必须在 MySQL 和 Redis 都启动后才能成功启动
4. **前端** 最后启动，因为前端依赖后端 API

---

## 🐛 常见问题排查

### 1. MySQL 无法连接

```bash
# 检查 MySQL 服务状态
# Windows: 打开"服务"管理器 → 找到 MySQL80 → 检查状态

# 启动 MySQL 服务
net start MySQL80
```

### 2. Redis 连接失败

```bash
# 检查 Redis 是否运行
redis-cli -a cyf12345 PING

# 如果失败，手动启动
redis-server --requirepass cyf12345

# 或者查看 Redis 日志
```

### 3. 后端启动失败

```bash
# 检查端口占用
netstat -ano | findstr :8080

# 检查数据库连接
mysql -u root -pcyf12345 -e "SHOW DATABASES LIKE 'ancient_books_db'"

# 检查 Redis 连接
redis-cli -a cyf12345 PING

# 查看详细错误日志
mvn spring-boot:run 2>&1 | tee /tmp/spring-boot.log
```

### 4. 前端请求 401

```bash
# 检查是否已登录
# 打开浏览器控制台 → Application → Local Storage → 查看是否有 token

# 如果没有 token，先登录
POST /api/auth/login
Body: { username: 'admin', password: 'admin123' }

# 保存返回的 token 到 localStorage
localStorage.setItem('token', response.data.token)
```

### 5. 前端请求 CORS 错误

**检查后端 CORS 配置：**

```yaml
# application.yml
spring:
  cors:
    allowed-origins: "http://localhost:5173"  # 前端地址
```

或在浏览器禁用 CORS（仅开发环境）：

```bash
# Chrome 启动参数
chrome.exe --disable-web-security --user-data-dir=./chrome-temp
```

---

## 📝 完整启动脚本

### Windows Batch 脚本

**文件：`start-all.bat`**

```batch
@echo off
echo ========================================
echo  古籍分析 Agent - 启动脚本
echo ========================================
echo.

echo [1/4] 启动 MySQL...
net start MySQL80 >nul 2>&1
if %errorlevel% == 0 (
    echo ✅ MySQL 已启动
) else (
    echo ⚠️ MySQL 启动失败，请手动检查
)

echo.
echo [2/4] 启动 Redis...
start /B redis-server --requirepass cyf12345
timeout /t 2 /nobreak >nul
echo ✅ Redis 已启动

echo.
echo [3/4] 启动后端服务...
start /B mvn spring-boot:run -f D:/code/ancinetbooks-Analyse/ancientbooks-pc-backend/pom.xml
timeout /t 10 /nobreak >nul
echo ✅ 后端已启动

echo.
echo [4/4] 启动前端服务...
start /B npm run dev -f D:/code/ancinetbooks-Analyse/ancientbooks-analyse-pc-frontend
timeout /t 5 /nobreak >nul
echo ✅ 前端已启动

echo.
echo ========================================
echo  启动完成！
echo ========================================
echo.
echo 后端地址：http://localhost:8080/api
echo 前端地址：http://localhost:5173
echo MySQL：127.0.0.1:3306（密码：cyf12345）
echo Redis：127.0.0.1:6379（密码：cyf12345）
echo.
echo 登录账号：admin / admin123
echo.
pause
```

### 使用方式

```bash
# 双击运行
start-all.bat
```

---

## ✅ 检查清单

启动前请确认：

- [ ] MySQL 已安装并启动
- [ ] Redis 已安装并启动
- [ ] 数据库已初始化（执行了 schema.sql）
- [ ] 后端配置文件正确（密码、端口）
- [ ] 前端环境变量配置正确
- [ ] 端口 3306、6379、8080、5173 未被占用

---

**如果有任何问题，请查看：**
- 后端日志：`/tmp/spring-boot.log`
- MySQL 日志：`C:\ProgramData\MySQL\MySQL Server 8.0\Data\*.err`
- Redis 日志：Redis 控制台输出
- 前端日志：浏览器控制台（F12）
